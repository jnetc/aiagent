<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title %>
  </title>
  <link rel="stylesheet" href="/static/css/global.css">
  <link rel="stylesheet" href="/static/css/home.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  <script src="/static/js/main.js" defer></script>
</head>

<body>
  <%- include('partials/navigation') %>
  <main class="main-content">
    <div class="hero-section">
      <div class="hero-content">
        <h1>AI-Powered NFT Analytics for Zora</h1>
        <p>Discover trending artists and collections before the market catches on. Get AI-driven insights from social
          signals, smart followers, and market data.</p>

        <div class="hero-cta">
          <% if (!isLoggedIn) { %>
          <a href="/login" class="btn btn-primary btn-large">Get Started Free</a>
          <a href="/pricing" class="btn btn-outline btn-large">View Pricing</a>
          <% } else { %>
          <a href="/analytics" class="btn btn-primary btn-large">View Analytics</a>
          <% if (!user.pro) { %>
          <a href="/pricing" class="btn btn-outline btn-large">Upgrade to Pro</a>
          <% } %>
          <% } %>
        </div>
      </div>
    </div>

    <!-- Features Section -->
    <div class="features-section">
      <div class="container">
        <h2>Why Zora AI Agent?</h2>
        <div class="features-grid">
          <div class="feature-card">
            <i class="fas fa-chart-line"></i>
            <h3>Real-time Analytics</h3>
            <p>Track NFT collections, artist metrics, and market movements in real-time across Zora and other platforms.
            </p>
          </div>

          <div class="feature-card">
            <i class="fas fa-brain"></i>
            <h3>AI Recommendations</h3>
            <p>GPT-powered insights analyze social signals, follower growth, and smart money movements to identify
              trends
              early.</p>
          </div>

          <div class="feature-card">
            <i class="fas fa-users"></i>
            <h3>Social Signals</h3>
            <p>Monitor Twitter follower growth, engagement rates, and track when influential collectors follow new
              artists.
            </p>
          </div>

          <div class="feature-card">
            <i class="fas fa-eye"></i>
            <h3>Early Detection</h3>
            <p>Spot promising artists and collections before they trend, giving you the edge in the fast-moving NFT
              market.
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- Demo Section -->
    <div class="demo-section">
      <div class="container">
        <h2>Live Analytics Preview</h2>
        <p>Here's what our AI is currently tracking on Zora:</p>

        <div class="demo-cards-container">
          <div class="analytics-grid">
            <% demoCards.forEach(card => { %>
            <div class="analytics-card demo">
              <div class="card-header">
                <div class="artist-info">
                  <div class="artist-avatar"
                    style="background: linear-gradient(135deg, #6366f1, #8b5cf6); color: white; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 1.2rem;">
                    <%= card.artist.displayName.charAt(0).toUpperCase() %>
                  </div>
                  <div>
                    <h4><%= card.artist.displayName %></h4>
                    <p>@<%= card.artist.username %></p>
                  </div>
                </div>
                <div class="platform-badge">
                  <i class="fas fa-cube"></i>
                  <%= card.collection.platform %>
                </div>
              </div>

              <div class="card-content">
                <h3><%= card.collection.name %></h3>

                <div class="metrics-grid">
                  <div class="metric">
                    <span class="metric-label">Followers</span>
                    <span class="metric-value">
                      <%= card.metrics.followers.toLocaleString() %>
                      <small class="change positive">+<%= card.metrics.followersChange24h %></small>
                    </span>
                  </div>

                  <div class="metric">
                    <span class="metric-label">Market Cap</span>
                    <span class="metric-value blurred">***</span>
                  </div>

                  <div class="metric">
                    <span class="metric-label">Volume 24h</span>
                    <span class="metric-value blurred">***</span>
                  </div>

                  <div class="metric">
                    <span class="metric-label">Smart Followers</span>
                    <span class="metric-value blurred">***</span>
                  </div>
                </div>

                <div class="ai-recommendation">
                  <i class="fas fa-robot"></i>
                  <p><%= card.aiRecommendation %></p>
                </div>

                <div class="card-tags">
                  <% card.tags.forEach(tag => { %>
                  <span class="tag"><%= tag %></span>
                  <% }) %>
                </div>
              </div>
            </div>
            <% }) %>
          </div>

          <div class="demo-cards-overlay">
            <div class="demo-upgrade-cta">
              <h4>🔓 See Full Data</h4>
              <p>Unlock complete analytics, AI insights, and real-time data</p>
              <a href="<% if (isLoggedIn) { %>/pricing<% } else { %>/login<% } %>" class="btn btn-primary">
                <% if (isLoggedIn) { %>
                <% if (user.pro) { %>Already Pro!<% } else { %>Upgrade to Pro<% } %>
                <% } else { %>
                Sign Up Free
                <% } %>
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Stats Section -->
    <div class="stats-section">
      <div class="container">
        <div class="stats-grid">
          <div class="stat">
            <h3>1,247</h3>
            <p>Projects Tracked</p>
          </div>
          <div class="stat">
            <h3>23</h3>
            <p>Trending Today</p>
          </div>
          <div class="stat">
            <h3>$8.5M</h3>
            <p>Volume 24h</p>
          </div>
          <div class="stat">
            <h3>+184%</h3>
            <p>Top Gainer</p>
          </div>
        </div>
      </div>
    </div>
  </main>

  <%- include('partials/footer') %>


</body>

</html>
